import 'package:flutter/material.dart';

class Constants {
  // Colors used in app
  static var primaryColor = Color.fromRGBO(130, 222, 2, 0.658);
  // static var primaryColor = Colors.purple;
  static var blackColor = Color.fromRGBO(231, 197, 163, 0.655);
  static var buttonColor = Colors.yellow;
}
